sudo: required
services:
  - docker
before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - docker build -t surath/sample-app -f Dockerfile.dev .
script:
  - docker run -e CI=true surath/sample-app npm run test
